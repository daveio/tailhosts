# frozen_string_literal: true

RSpec.describe Tailhosts do
  it "has a version number" do
    expect(Tailhosts::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
